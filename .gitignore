/__pycache__
/.idea
/.venv

# User-/machine-local settings — persistent across sessions, never committed.
/app_settings.json
/panel_settings.json
/theme_colors.json

# Local junction to the Unity project; keep Unity files in that project.
/clothSimulation/
